# Optimization attempt log

Format per mission spec: one row per attempt, kept AND reverted both logged
so nothing gets retried blind.

| # | Change | Stage | Time before -> after (total) | VRAM peak before -> after | Quality (PSNR/SSIM/LPIPS \| audio LSD/MCD) | Human check | Verdict |
|---|--------|-------|-------------------------------|---------------------------|----------------------------------------------|--------------|---------|
